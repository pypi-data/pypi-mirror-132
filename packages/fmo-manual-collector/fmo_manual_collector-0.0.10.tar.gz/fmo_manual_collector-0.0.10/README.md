# fmo_manual_collector

## conda environment
-  creaet environment
```sh
conda clean --all;
conda env create --file environment.yml;
```

- update environment
```sh
conda env update --file environment.yml --prune ;
```