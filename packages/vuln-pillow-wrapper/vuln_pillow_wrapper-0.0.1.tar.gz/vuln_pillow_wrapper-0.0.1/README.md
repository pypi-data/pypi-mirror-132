# Vulnerable pillow wrapper
This package is used to demostrate why software composition problems can be non trivial solve. This package has a vulnerable dependency but it is "not directly" vulnerable.
